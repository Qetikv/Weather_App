name: flutter_weather_app
description: "A Flutter weather app project."
version: 1.0.0

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/clear.png
    - assets/cloudy.png
    - assets/rainy.png
    - assets/default.png
